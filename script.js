// const slider = document.querySelector(".banner__slider");
// const images = slider.children;
// let index = 0;

// function slide() {
//     index++;
//     if (index >= images.length) {
//         index = 0; // Reset to first image after the last one
//     }
//     slider.style.transform = `translateY(-${index * 250}px)`; // Move by the height of one image
// }

// setInterval(slide, 3000); // Auto-scroll every 3 seconds